{
  "private": true,
  "scripts": {
    "start": "cross-env BUILD_ENV=development PROXY_ENV=development ENV=development umi dev",
    "start:pro": "cross-env BUILD_ENV=development PROXY_ENV=production ENV=development umi dev",
    "build": "cross-env BUILD_ENV=production umi build",
    "build:dev": "cross-env BUILD_ENV=development umi build",
    "test": "umi test",
    "lint": "eslint {src,mock,tests}/**/*.{js,jsx} --fix",
    "precommit": "lint-staged"
  },
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "apollo-lint": "^0.0.5",
    "cross-env": "^5.2.0",
    "husky": "^0.14.3",
    "lint-staged": "^7.2.2",
    "react-test-renderer": "^16.7.0",
    "umi": "^2.7.7",
    "umi-plugin-polyfill": "^0.2.0",
    "umi-plugin-react": "^1.8.4"
  },
  "lint-staged": {
    "*.{js,jsx}": [
      "prettier --write",
      "eslint --fix",
      "git add"
    ]
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
